"""Wayly backend library modules.

These hold pure helpers + reference constants used by the route handlers
in ``server.py``. The plan is to gradually migrate route handlers here in
focused iterations, see ``server.py`` for the entry point.
"""
