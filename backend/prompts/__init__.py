"""Backend prompts package.

Extracted from `server.py` and `agents.py` as part of the CPR-1 build so
prompt versions are trackable in git and reused by tests without pulling
the entire server module.
"""
