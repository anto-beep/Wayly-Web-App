"""CPR-1 test fixtures — care-plan PDF and text fixtures.

Kept as builder scripts rather than committed binary blobs so the CI can
regenerate deterministically and reviewers can inspect the source-of-truth
in git.
"""
